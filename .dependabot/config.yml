version: 1
update_configs:
  - package_manager: "submodules"
    directory: "./"
    update_schedule: "daily"
    allowed_updates:
      - match:
          dependency_name: "2-vkutuev-test"
    ignored_updates:
      - match:
          dependency_name: "2-grading-tests"
